import React from "react";
import { makeStyles } from "@mui/styles";
import TestimonialStyles from "@/public/jss/next-jss-components/testimonialStyles";

const Testimonial = () => {
  const classes = makeStyles(TestimonialStyles)();
  return <div className={classes.main}>
    
  </div>;
};

export default Testimonial;
